services:
  mindmap:
    build: .
    container_name: mindmap
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "127.0.0.1:3000:3000"   # change the host-side port if 3000 is already in use
    volumes:
      - ./db:/app/db
